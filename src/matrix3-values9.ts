type Matrix3Values9 = [
    number, number, number,
    number, number, number,
    number, number, number,
]

/**
 * @todo replace with export default when typescript will support it
 */
export { Matrix3Values9 as default }
